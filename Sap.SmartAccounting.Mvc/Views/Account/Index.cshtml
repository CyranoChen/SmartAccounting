@model Sap.SmartAccounting.Mvc.Models.SettingModels.AccountListDto

@section Styles {
    <link rel="stylesheet" href="~/Content/DataTables/css/dataTables.bootstrap.min.css">
}

@section Scripts {
    <!-- DataTables -->
    <script src="~/Scripts/Datatables/jquery.dataTables.min.js"></script>
    <script src="~/Scripts/Datatables/dataTables.bootstrap.min.js"></script>
    <script type="text/javascript">
        $(function () {
            $("#tbl-company").DataTable({
                "paging": true,
                "lengthChange": false,
                "searching": false,
                "ordering": true,
                "info": true,
                "autoWidth": true
            });
        });
    </script>
}

<!-- Content Wrapper. Contains page content -->
<div class="content-wrapper">
    <!-- Content Header (Page header) -->
    <section class="content-header">
        <h1>
            <b><i class="fa fa-user"></i> Preset Accounts</b>
            <small></small>
        </h1>
        <ol class="breadcrumb">
            <li><a href="@Url.Action("Index","Home")"><i class="fa fa-dashboard"></i> Console</a></li>
            <li><a href="#">Setting</a></li>
            <li class="active">Account</li>
        </ol>
    </section>
    <!-- Main content -->
    <section class="content">
        <div class="row">
            <div class="col-xs-12">
                <div class="box">
                    <div class="box-body">
                        <div class="mailbox-controls">
                            <!-- /.btn-group -->
                            <div class="btn-group">
                                <button type="button" class="btn btn-default"
                                        onclick="window.location.href = '@Url.Action("Create", "Account")'">
                                    <i class="fa fa-plus-circle"></i>
                                </button>
                            </div>
                            <!-- /.box-tools -->
                        </div>
                        <table id="tbl-company" class="table table-bordered table-striped">
                            <thead>
                                <tr>
                                    <th>B1Id</th>
                                    <th>CompanyCode</th>
                                    <th>CompanyName</th>
                                    <th>Input / Output</th>
                                    <th>Operation</th>
                                </tr>
                            </thead>
                            @if (Model.Accounts != null && Model.Accounts.Count > 0)
                            {
                                <tbody>
                                    @foreach (var m in Model.Accounts)
                                    {
                                        <tr>
                                            <td>@m.B1Id</td>
                                            <td>@m.AccountCode</td>
                                            <td>@m.AccountName</td>
                                            <td>@m.IsIncoming</td>
                                            <td>
                                                <button type="button" class="btn btn-danger btn-xs" id="@m.ID">
                                                    <i class="fa fa-remove"></i> delete
                                                </button>
                                            </td>
                                        </tr>
                                    }
                                </tbody>
                            }
                        </table>
                    </div>
                    <!-- /.box-body -->
                </div>
                <!-- /.box -->
            </div>
            <!-- /.col -->
        </div>
        <!-- /.row -->
    </section>
    <!-- /.content -->
</div>
<!-- /.content-wrapper -->
